name: Test Dify Secrets

on:
  workflow_dispatch:  # 手动触发
    inputs: {}         # 不需要额外参数

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    env:
      DIFY_BASE_URL: ${{ secrets.DIFY_BASE_URL }}
      DIFY_TOKENS: ${{ secrets.DIFY_TOKENS }}
      DIFY_INPUTS: ${{ secrets.DIFY_INPUTS }}
    steps:
      - name: 检查 DIFY_BASE_URL
        run: |
          if [ -z "$DIFY_BASE_URL" ]; then
            echo "❌ DIFY_BASE_URL 未设置或为空"
            exit 1
          else
            echo "✅ DIFY_BASE_URL 已加载：$DIFY_BASE_URL"
          fi

      - name: 检查 DIFY_TOKENS
        run: |
          if [ -z "$DIFY_TOKENS" ]; then
            echo "❌ DIFY_TOKENS 未设置或为空"
            exit 1
          else
            echo "✅ DIFY_TOKENS 已加载（内容隐藏）"
          fi

      - name: 检查 DIFY_INPUTS
        run: |
          if [ -z "$DIFY_INPUTS" ]; then
            echo "❌ DIFY_INPUTS 未设置或为空"
            exit 1
          else
            echo "✅ DIFY_INPUTS 已加载：$DIFY_INPUTS"
          fi

      - name: 所有 Secrets 检查完成
        run: echo "🎉 所有 Dify Secrets 已正确配置并加载成功！"
